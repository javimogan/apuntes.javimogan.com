name: Ejecutar Prebuild y Commit

on:
  repository_dispatch:
    types: [update_content]

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v3
        with:
            submodules: recursive  # Fetch the Docsy theme
            fetch-depth: 0         # Fetch all history for .GitInfo and .Lastmod
      # - name: Install, build, and upload your site output
      #   uses: withastro/action@v2
      - name: 🛠️ Instalar dependencias
        run: npm install
      # - name: Setup Node
      #   uses: actions/setup-node@v3
      #   with:
      #     node-version: '20'
      #     cache: 'npm'
      - name: ⚙️ Ejecutar Prebuild
        run: TOKEN_GITHUB=${{ secrets.TOKEN_GITHUB }} && npm run prebuild

      # - name: 📝 Comprobar cambios en Git
      #   run: |
      #     git config --global user.name "GitHub Actions"
      #     git config --global user.email "diazalonsojavi@gmail.com"

      #     # Verificar si hay cambios
      #     if [[ -n $(git status --porcelain) ]]; then
      #       git add .
      #       git commit -m "🚀 Auto-update after prebuild"

      #       # Autenticación manual con token para hacer push
      #       git remote set-url origin https://x-access-token:${{ secrets.TOKEN_GITHUB }}@github.com/${{ github.repository }}.git
      #       git push origin HEAD:main
      #     else
      #       echo "✅ No hay cambios para commitear."
      #     fi
